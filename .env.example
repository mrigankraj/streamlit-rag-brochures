# Only needed if you keep using OpenAI to generate answers (LLM).
# You still don't need OpenAI for embeddings (they're local).
OPENAI_API_KEY=""

# Local FAISS files
FAISS_INDEX_PATH=./faiss_index.index
FAISS_DOCS_PATH=./faiss_docs.pkl

# Sentence-transformers model to use
SENTENCE_MODEL=all-MiniLM-L6-v2

# Download folder used by downloader
DOWNLOAD_DIR=./downloads
